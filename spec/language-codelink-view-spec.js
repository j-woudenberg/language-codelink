'use babel';

import LanguageCodelinkView from '../lib/language-codelink-view';

describe('LanguageCodelinkView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
